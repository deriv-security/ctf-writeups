<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Gate of Broken Names</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Creepster&family=Cinzel:wght@400;600;700&family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
  
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            'halloween-orange': '#FF6B35',
            'halloween-purple': '#6B2D5C',
            'halloween-black': '#0A0A0A',
            'halloween-gray': '#1A1A1A',
            'mystic-gold': '#D4AF37',
            'ghost-white': '#F8F8F8',
            'blood-red': '#8B0000',
            'fog-gray': '#2A2A2A'
          },
          fontFamily: {
            'creepy': ['Creepster', 'cursive'],
            'gothic': ['Cinzel', 'serif'],
            'body': ['Poppins', 'sans-serif']
          },
          animation: {
            'float': 'float 6s ease-in-out infinite',
            'pulse-slow': 'pulse 4s cubic-bezier(0.4, 0, 0.6, 1) infinite',
            'flicker': 'flicker 3s linear infinite',
            'fog': 'fog 20s ease-in-out infinite',
            'swing': 'swing 3s ease-in-out infinite'
          },
          keyframes: {
            float: {
              '0%, 100%': { transform: 'translateY(0px)' },
              '50%': { transform: 'translateY(-20px)' }
            },
            flicker: {
              '0%, 100%': { opacity: '1' },
              '41.99%': { opacity: '1' },
              '42%': { opacity: '0' },
              '43%': { opacity: '0' },
              '43.01%': { opacity: '1' },
              '47.99%': { opacity: '1' },
              '48%': { opacity: '0' },
              '49%': { opacity: '0' },
              '49.01%': { opacity: '1' }
            },
            fog: {
              '0%': { transform: 'translateX(-50%) translateY(0)' },
              '50%': { transform: 'translateX(-50%) translateY(-20px)' },
              '100%': { transform: 'translateX(-50%) translateY(0)' }
            },
            swing: {
              '0%, 100%': { transform: 'rotate(-3deg)' },
              '50%': { transform: 'rotate(3deg)' }
            }
          },
          backgroundImage: {
            'halloween-gradient': 'linear-gradient(135deg, #0A0A0A 0%, #1A1A1A 50%, #2A1A1A 100%)',
            'mystic-glow': 'radial-gradient(circle, rgba(212,175,55,0.1) 0%, transparent 70%)'
          }
        }
      }
    }
  </script>
  
  <link rel="stylesheet" href="/css/main.css">
</head>
<body class="bg-halloween-black min-h-screen font-body text-ghost-white">
  <%- include('../partials/background') %>
  
  <% if (user) { %>
    <%- include('../partials/header', { user, activePage }) %>
  <% } %>
  
  <div class="relative z-10">
    <%- body %>
  </div>
  
  <script src="/js/effects.js"></script>
</body>
</html>

